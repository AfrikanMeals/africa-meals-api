import { MultipartToJsonPipe } from './multipart-to-json.pipe';

describe('MultipartToJsonPipe', () => {
  it('should be defined', () => {
    expect(new MultipartToJsonPipe()).toBeDefined();
  });
});
